// Notice that every statement (usually every line of code) has a semicolon after it.

// Create a variable called "age" and set it to 21
// Try changing the value of age to a different number, or a string.
// Click run again, and notice how it always outputs the value of age.
var age = 21;

// Print "age" to the console
console.log(age);
